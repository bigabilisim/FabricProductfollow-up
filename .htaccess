Options -Indexes
RewriteEngine On

RewriteRule ^(config|cron|database|src|storage|tests|vendor)(/|$) - [F,L]
RewriteRule ^(composer\.(json|lock)|README\.md|VERSION)$ - [F,L]

RewriteCond %{REQUEST_URI} !/public/
RewriteRule ^(.*)$ public/$1 [L]
